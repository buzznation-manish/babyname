import NamesBrowser from '@/components/names-browser';

export default function NamesPage() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-amber-50 via-rose-50 to-sky-50 px-6 py-12">
      <NamesBrowser />
    </main>
  );
}
